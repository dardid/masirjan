{% extends 'front.html.twig' %}
{% block title %}{{page.title}}{% endblock %}
{% block pageBody %}
<div class="container">
    <div class="row text-justify">
        <div class="col-12 py-5">
            <h1>{{ page.title }}</h1>
            <p class="">{{ page.body | raw }}</p>
        </div>
    </div>
{% endblock %}
